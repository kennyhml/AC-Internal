#pragma once

#include <Windows.h>
#include <memory>

namespace SDK
{
	struct Player;
	struct Weapon;
	struct WeaponData;
	struct ReserveData;

	enum class MovementFlags;
	enum class Team;
	enum class Status;
	enum class GameMode;
}